services:
  netmon:
    image: netmon:latest
    restart: unless-stopped
    # Host networking is required for network monitoring visibility.
    network_mode: host
    # Grant only the minimum capability required for network operations.
    cap_add:
      - NET_ADMIN
    volumes:
      - .:/app
